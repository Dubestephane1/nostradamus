# 1. Force trailing slash if missing (301 redirect)
/c*/*   /c*/*/   301!

# 2. Rewrite folder-with-trailing-slash → internal index.html
# Use :splat placeholder to capture the matched path and reuse it
/c*/*/*   /:splat/index.html   200!

# Optional: Catch-all 404 (if you have a custom 404.html)
/*   /404.html   404