# 1. Force trailing slash if missing (301 redirect)
/c*/*   /c*/*/   301!

# 2. Rewrite folder-with-trailing-slash → internal index.html (200 rewrite, URL stays clean)
/c*/*/   /c*/*/index.html   200

# Optional: Catch-all for unmatched paths → custom 404 if you have 404.html
/*   /404.html   404